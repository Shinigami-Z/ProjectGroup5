<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">G5</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title == "Home") { %>
              <a class="nav-link active" href="/home">
            <% } else { %>
              <a class="nav-link" href="/home">
            <% } %>
              <i class="fa-solid fa-lg fa-house-laptop"></i> Home</a>
          </li>
          </li>
          <li class="nav-item">
            <% if(title == "Inventory") { %>
              <a class="nav-link active" href="/InventoryRT">
            <% } else { %>
              <a class="nav-link" href="/InventoryRT">
            <% } %>
                  <i class="fa-solid fa-calendar-days fa-lg"></i> Inventory</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
